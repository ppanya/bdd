meta {
  name: ดึง user ที่ไม่มีอยู่ ควรได้ 404
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/users/9999
  body: none
  auth: none
}

headers {
  Prefer: code=404
}
tests {
  test("status is 404", function() {
    expect(res.status).to.equal(404);
  });
}
