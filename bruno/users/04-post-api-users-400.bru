meta {
  name: สร้าง user ที่ข้อมูลไม่ครบ ควรได้ 400
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/users
  body: json
  auth: none
}

headers {
  Prefer: code=400
}
body:json {
  {
    "username": ""
  }
}
tests {
  test("status is 400", function() {
    expect(res.status).to.equal(400);
  });
}
